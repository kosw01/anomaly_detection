  ┌────────────────────────────────────────────────────────────┐
  │ 1. 채널 메타데이터 로드                                    │
  │   - channel_info/channel_info.csv                          │
  │   - br_name, channel_name, channel_type, superposition     │
  │   - result_May, result_June, result_July (라벨)            │
  └────────────────────────────────────────────────────────────┘
                                │
                                ▼
  ┌────────────────────────────────────────────────────────────┐
  │ 2. 시계열 데이터 로드                                      │
  │   월별 폴더 구조                                            │
  │   '2025-05{br_name}/{br_name}_통계데이터.csv'               │
  │   '2025-06{br_name}/{br_name}_통계데이터.csv'               │
  │   '2025-07{br_name}/{br_name}_통계데이터.csv'               │
  │   → {채널명}_AVG 컬럼 추출                                 │
│   → 결측치 처리, 정규화, (월별처리, 3개월 이어붙여처리          │
  └────────────────────────────────────────────────────────────┘
                                │
                                ▼
  ┌────────────────────────────────────────────────────────────┐
  │ 3. 입력 데이터 구성                                         │
  │   - X_ts: (채널 수, 시계열 길이, 1)                         │
  │   - meta_encoded: (채널 수, 메타데이터 인코딩 차원)         │
  │   - y: 정상/비정상/이상 (One-Hot 라벨)                      │
  └────────────────────────────────────────────────────────────┘
                                │
                                ▼
  ┌────────────────────────────────────────────────────────────┐
  │ 4. 모델 설계 (다중입력)                                     │
  │   시계열 입력 (Conv1D 블록)                                 │
  │   메타데이터 입력 (Dense 블록)                              │
  │   특징 병합 → Fully Connected → Softmax(3)                  │
  └────────────────────────────────────────────────────────────┘
                                │
                                ▼
  ┌────────────────────────────────────────────────────────────┐
  │ 5. 학습 & 평가                                              │
  │   - Train / Validation / Test Split                        │
  │   - EarlyStopping, ModelCheckpoint                         │
  │   - Accuracy / Loss 모니터링                               │
  └────────────────────────────────────────────────────────────┘
                                │
                                ▼
  ┌────────────────────────────────────────────────────────────┐
  │ 6. 모델 저장 & 예측                                         │
  │   - bridge_sensor_classifier.h5                            │
  │   - 새로운 데이터에 대한 정상/비정상/이상 분류              │
  └────────────────────────────────────────────────────────────┘
